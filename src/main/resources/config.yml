plugin:
  debug: false
  delay: 10
  max-db-connection: 8
  omit-player-death: false

sync:
  health: true
  food: true
  exp: true
  inventory: true
  chest: true
  potion: true

transfer-origin: false
transfer:
  # Execute while transfer done, by console. %player_name% supported here, multi command split by '\n'
  extra-notify-command: ''

kick-load-failed: true

message:
  kick_load: Your game data loading error, please contact the operator
